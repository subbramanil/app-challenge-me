<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/mainDrawerLayout"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="com.utd.learning.mycontacts.ui.MainActivity">

        <include
            android:id="@+id/app_bar"
            layout="@layout/app_bar" />

        <Button
            android:id="@+id/testBtn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/app_bar"
            android:elevation="4dp"
            android:text="@string/test_btn" />
    </RelativeLayout>

    <fragment
        android:layout_width="@dimen/nav_drawer_width"
        android:layout_height="match_parent"
        app:layout="@layout/fragment_navigatin_drawer"
        android:layout_gravity="start"
        android:name="com.utd.learning.mycontacts.ui.NavigationDrawerFragment"
        tools:layout="@layout/fragment_navigatin_drawer"
        android:id="@+id/navDrawerFragment"/>

</android.support.v4.widget.DrawerLayout>

